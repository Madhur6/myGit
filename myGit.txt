--------------------- ⭐⭐ ✨ GIT SETUP (WINDOWS 10/11) ✨ ⭐⭐ ---------------------

💡: INSTALL GIT

- Download Git for Windows → Select **64-bit Git.exe**
- Install → Click "Next" → Finish
- Open CMD / PowerShell
- Verify installation:

    git --version




--------------------------------------------------------------------------------------------

💡: GENERATE SSH KEY 🔐 (RECOMMENDED AUTH METHOD)

- Open CMD / PowerShell
- Generate SSH key (replace with your GitHub email):

    ssh-keygen -t ed25519 -C "your_email@example.com"

- When prompted:
    > Enter file location → Press **Enter** (default)
    > Enter passphrase → Optional but recommended
    > Re-enter passphrase

- Start SSH agent:

    eval "$(ssh-agent -s)"
    ssh-add ~/.ssh/id_ed25519

- Copy public key:

    cat ~/.ssh/id_ed25519.pub

- GitHub → Settings → SSH & GPG Keys → **New SSH Key** → Paste key

⚠️ Important:
- If you use SSH keys → ALWAYS use SSH repo URLs
- Do NOT mix HTTPS + SSH





--------------------------------------------------------------------------------------------

💡 CLONING & WORKING WITH A FORK 🚀

- Clone your fork:

    git clone git@github.com:<your-username>/<repo>.git
    cd <repo>

- Add upstream (original repository):

    git remote add upstream git@github.com:<original-owner>/<repo>.git
    git remote -v


💡 AFTER MAKING CHANGES 👇

    git add .
    git commit -m "Your commit message"

(Optional but recommended before pushing)

    git fetch upstream
    git rebase upstream/main

    git push origin main






--------------------------------------------------------------------------------------------

💡 CLONING & WORKING DIRECTLY (NO FORK) 🚀

Use this when you **HAVE push access** to the repository.

You can clone the repository using **SSH (recommended)** or **HTTPS**.


🔐 OPTION 1: CLONE USING SSH (RECOMMENDED)

Best choice if you have an SSH key configured.
No username/password prompts when pushing.

    git clone git@github.com:<owner>/<repo>.git
    cd <repo>


🌐 OPTION 2: CLONE USING HTTPS

Use this if SSH is not set up.
You will need a **Personal Access Token (PAT)** when pushing.

    git clone https://github.com/<owner>/<repo>.git
    cd <repo>

____________________________________________________________________________

⭐ VERIFY & CONFIGURE REMOTES 👇

- Check configured remotes:

    git remote -v

Expected output (SSH example):

    origin  git@github.com:<owner>/<repo>.git (fetch)
    origin  git@github.com:<owner>/<repo>.git (push)

Expected output (HTTPS example):

    origin  https://github.com/<owner>/<repo>.git (fetch)
    origin  https://github.com/<owner>/<repo>.git (push)

____________________________________________________________________________

⭐ ADD / FIX REMOTE (IF NEEDED) 👇

If `origin` is missing or incorrect:

    git remote add origin git@github.com:<owner>/<repo>.git

OR (HTTPS)

    git remote add origin https://github.com/<owner>/<repo>.git


If `origin` already exists but is wrong:

    git remote set-url origin git@github.com:<owner>/<repo>.git

OR (HTTPS)

    git remote set-url origin https://github.com/<owner>/<repo>.git

____________________________________________________________________________


IMPORTANT NOTES ⚠️

- Email is **NOT used for cloning or authentication**
- SSH uses **SSH keys**
- HTTPS uses **username + Personal Access Token**
- Prefer **SSH** for long-term development
- Avoid pushing directly to `main` unless explicitly allowed








--------------------------------------------------------------------------------------------

💡: FIX "AUTHOR IDENTITY UNKNOWN" ERROR 😫

(Error occurs during first commit)

Fix:

    git config --global user.name "Your Name"
    git config --global user.email "you@example.com"

Verify:

    git config --list




--------------------------------------------------------------------------------------------

💡: INITIALIZE GIT REPOSITORY FROM LOCAL FOLDER 📂

1) Create an **empty repository** on GitHub (no README)

2) Inside your project folder:

    cd <project-folder>
    git init
    git branch -M main

    git remote add origin git@github.com:<username>/<repo>.git
    git remote -v

    git add .
    git commit -m "Initial commit"
    git push -u origin main




--------------------------------------------------------------------------------------------

💡 DELETE A REMOTE BRANCH ❌ (USE WITH CAUTION)

⚠️ Do NOT delete `main` unless another branch is set as default on GitHub

    git push origin --delete <branch-name>




--------------------------------------------------------------------------------------------

💡 FETCHING UPDATES FROM UPSTREAM 🔄

    git fetch upstream
    git switch main
    git rebase upstream/main
    git push origin main




--------------------------------------------------------------------------------------------

💡 WORKING WITH NEW BRANCH FROM UPSTREAM 🌱

    git fetch upstream
    git switch -c new-branch-name upstream/new-branch-name
    git push -u origin new-branch-name




--------------------------------------------------------------------------------------------

💡 RESET TO A PREVIOUS COMMIT ⏪

- View commit history:

    git log --oneline

- Hard reset to specific commit:

    git reset --hard <commit-hash>

- Force push (⚠️ rewrites history):

    git push origin <branch-name> --force




--------------------------------------------------------------------------------------------

💡 SWITCH COMPLETELY TO ANOTHER BRANCH 🔁

    git fetch upstream
    git switch -c development upstream/development

(Optional: overwrite current branch)

    git switch main
    git reset --hard upstream/development
    git push origin main --force




--------------------------------------------------------------------------------------------

💡 WHEN YOU ARE "COMMITS AHEAD / BEHIND" (MERGE CONFLICT FIX) ⚠️

Situation:
- Local branch diverged from upstream

Fix (discard local changes):

    git switch main
    git reset --hard upstream/main
    git push origin main --force

⚠️ Never do this on shared branches without team approval




--------------------------------------------------------------------------------------------

💡 KEEP ONLY ONE COMMIT (SQUASH HISTORY) 🧹

    git add .
    git commit --amend

- Save & exit editor
- Force push:

    git push origin main --force




--------------------------------------------------------------------------------------------

💡 RECOVER LOST COMMITS (LIFESAVER 🛟)

    git reflog

Restore commit:

    git reset --hard <commit-hash>

Restore files only (without moving HEAD):

    git checkout <commit-hash> -- .




--------------------------------------------------------------------------------------------

💡 CREATE / PUSH A NEW REPOSITORY FROM CLI 🆕

Create new repo locally:

    echo "# ProjectName" >> README.md
    git init
    git branch -M main
    git add README.md
    git commit -m "Initial commit"
    git remote add origin git@github.com:<username>/<repo>.git
    git push -u origin main

Push existing project:

    git remote add origin git@github.com:<username>/<repo>.git
    git branch -M main
    git push -u origin main




--------------------------------------------------------------------------------------------

💡 WINDOWS DEVELOPER MODE 🧑‍💻 (OPTIONAL)

    Win + R
    Type: ms-settings:developers
    Enable Developer Mode




--------------------------------------------------------------------------------------------

💡 FLUTTER PACKAGE MANAGEMENT 🔄

    flutter pub outdated
    flutter pub upgrade --major-versions




--------------------------------------------------------------------------------------------

💡 NPM & FIREBASE SETUP (WINDOWS SAFE) 🔥

Update npm:

    npm install -g npm@latest

Install Firebase CLI:

    npm install -g firebase-tools

Verify:

    firebase --version

Login:

    firebase login




--------------------------------------------------------------------------------------------

💡 FLUTTERFIRE SETUP 🔥

Activate CLI:

    dart pub global activate flutterfire_cli

⚠️ If command not found, add to PATH:

    %USERPROFILE%\AppData\Local\Pub\Cache\bin

Verify:

    flutterfire --version

Configure Firebase:

    flutterfire configure
    → Select app
    → Select platforms
    → Finish



--------------------------------------------------------------------------------------------

📁 LOCAL PATH NOTE:
    C:\Users\<USERNAME>\AppData\Roaming\sqldeveloper\23.1.1




--------------------- ⭐⭐ ✨           END             ✨ ⭐⭐ ---------------------